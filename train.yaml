model_name: "smooth_diff_unet"  # "diff_unet"
image_size: 96
spatial_size: 96
num_classes: 16
device: "cuda:0"
model_path: null  # "logs/amos/weights/epoch_800.pt"
pretrained: true
project_name: "smoothing-encoder-ver2"
wandb_name: "diff-unet"
use_wandb: true  # default=False
use_amp: true  # true
log_dir: "test"
max_epochs: 2000
batch_size: 15
num_workers: 2
loss_combine: "plus"
val_freq: 20000
num_gpus: 5
use_cache: true  # true

